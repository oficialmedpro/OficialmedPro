import React from 'react';
import './VendasPage.css';

const VendasAbas = ({ contexto, role }) => {
  return (
    <section className="tab card">
      <h2>Vendas – Negociação → Follow-up → Cadastro (Venda)</h2>
      <div className="hr"></div>
      <div style={{ textAlign: 'center', padding: 40 }}>
        <p style={{ color: 'var(--muted)' }}>Implementação em andamento...</p>
      </div>
    </section>
  );
};

export default VendasAbas;

